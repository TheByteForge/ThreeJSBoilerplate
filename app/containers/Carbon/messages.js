/*
 * Carbon Messages
 *
 * This contains all the text for the Carbon container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Carbon';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Carbon Container Component.',
  },
});
